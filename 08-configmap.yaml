# apiVersion: v1
# kind: ConfigMAP
# metadata:
#   name: nginx-config
# data:
#   course: devops
#   duration: "120hrs"

apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
data:
  course: devops
  duration: "120hrs"
